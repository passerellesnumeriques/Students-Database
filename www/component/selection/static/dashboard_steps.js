function dashboard_steps(container, steps, th_head_color){	var t = this;	t.table = document.createElement("table");	t.th_header = document.createElement("th");	t._init = function(){		t._setTableHeader();		t._setTableBody();		if(th_head_color == null)			th_head_color = "#808080";		setCommonStyleTable(t.table,t.th_header,th_head_color);		if(typeof(container) == "string")			container = document.getElementById(container);		container.appendChild(t.table);	}		t._setTableHeader = function(){		var tr = document.createElement("tr");		t.th_header.innerHTML = "Selection Process main steps";		tr.appendChild(t.th_header);		t.table.appendChild(tr);	}		/**	 * We only display the validated steps	 */	t._setTableBody = function(){		var no_step_validated = t._noStepValidated();		if(no_step_validated){			var tr = document.createElement("tr");			var td = document.createElement("td");			td.innerHTML = "<i>There is no validated step</i>";			tr.appendChild(td);			t.table.appendChild(tr);		} else {			for(var i = 0; i < steps.length; i++){				if(steps[i].value){					var td = document.createElement("td");					td.innerHTML = "- ";					td.innerHTML += steps[i].text;					var tr = document.createElement("tr");					tr.appendChild(td);					t.table.appendChild(tr);				}			}		}	}		t._noStepValidated = function(){		var no_step_validated = true;		for(var i = 0; i < steps.length; i++){			if(steps[i].value){				no_step_validated = false;				break;			}		}		return no_step_validated;	}		t._init();}